---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: tls
  namespace: tls-system
spec:
  interval: 10m0s
  chart:
    spec:
      chart: cert-manager
      version: ">=1.14.4 <1.15.0"
      sourceRef:
        kind: HelmRepository
        name: tls
        namespace: tls-system
      interval: 1m
  values:
    installCRDs: true
