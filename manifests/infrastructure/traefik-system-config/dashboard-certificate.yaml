---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: dashboard
  namespace: traefik-system
spec:
  commonName: traefik.omnilium.local
  secretName: dashboard-tls
  dnsNames:
    - traefik.omnilium.local
  issuerRef:
    kind: ClusterIssuer
    name: local
