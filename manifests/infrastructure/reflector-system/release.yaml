---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: reflector
  namespace: reflector-system
spec:
  interval: 10m0s
  chart:
    spec:
      chart: reflector
      version: ">=7.1.262 <7.2.0"
      sourceRef:
        kind: HelmRepository
        name: reflector
        namespace: reflector-system
      interval: 1m
  values:
    cron:
      enabled: true
    podLabels:
      app.omnilium.local/identifier: reflector
    resources: {}
    priorityClassName: medium
