---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: descheduler
  namespace: kube-system
spec:
  interval: 10m0s
  chart:
    spec:
      chart: descheduler
      version: ">=0.29.0 <0.30.0"
      sourceRef:
        kind: HelmRepository
        name: descheduler
        namespace: kube-system
      interval: 1m
  values: {}
