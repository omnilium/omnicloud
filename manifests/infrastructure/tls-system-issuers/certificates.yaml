---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: traefik-dashboard
  namespace: traefik-system
spec:
  commonName: traefik.talos.omnilium.local
  secretName: traefik-dashboard
  dnsNames:
    - traefik.talos.omnilium.local
  issuerRef:
    kind: ClusterIssuer
    name: local
