---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: traefik
  namespace: traefik-system
spec:
  interval: 10m0s
  chart:
    spec:
      chart: traefik
      version: ">=27.0.0 <28.0.0"
      sourceRef:
        kind: HelmRepository
        name: traefik
        namespace: traefik-system
      interval: 1m
  values:
    speaker:
      frr:
        enabled: true
