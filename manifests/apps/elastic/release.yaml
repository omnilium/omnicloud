---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: elastic-operator
  namespace: elastic
spec:
  interval: 10m0s
  chart:
    spec:
      chart: eck-operator
      version: ">=57.2.1 <57.3.0"
      sourceRef:
        kind: HelmRepository
        name: elastic
        namespace: elastic
      interval: 1m
  values: {}
