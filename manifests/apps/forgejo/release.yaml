---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: elastic-operator
  namespace: elastic
spec:
  interval: 10m0s
  chart:
    spec:
      chart: eck-operator
      version: ">=2.12.1 <2.13.0"
      sourceRef:
        kind: HelmRepository
        name: elastic
        namespace: elastic
      interval: 1m
  values: {}
