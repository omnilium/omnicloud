apiVersion: postgresql.cnpg.io/v1
kind: Pooler
metadata:
  name: flagsmith-cnpg-pooler
  namespace: flagsmith
spec:
  cluster:
    name: flagsmith-cnpg-cluster
  instances: 3
  type: rw
  pgbouncer:
    poolMode: session
    parameters:
      max_client_conn: "100"
      default_pool_size: "20"
