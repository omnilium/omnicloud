apiVersion: postgresql.cnpg.io/v1
kind: Pooler
metadata:
  name: onedev-cnpg-pooler
  namespace: onedev
spec:
  cluster:
    name: onedev-cnpg-cluster
  instances: 3
  type: rw
  pgbouncer:
    poolMode: session
    parameters:
      max_client_conn: "100"
      default_pool_size: "20"
